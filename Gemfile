source "https://rubygems.org"

# GitHub Pages (classic) builds this site with the github-pages gem.
# Plugins used (jekyll-seo-tag, jekyll-sitemap) are on the GH Pages allowlist.
gem "github-pages", group: :jekyll_plugins
gem "webrick"
